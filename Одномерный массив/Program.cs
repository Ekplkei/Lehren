using System;
using System.Linq;
namespace Одномерный_массив
{
    class Program
    {
        static void Main(string[] args)
        {
            /*int[] arr = new int[5];
            arr[4] = 37;*/
            int[] arr = {1,2,3,4,5};
            //int[] arr = Enumerable.Repeat(23, 5).ToArray(); // 23
            //int[] arr = Enumerable.Range(23, 5).ToArray(); //27
            //Console.WriteLine(arr[4]);            
            //for (int i = 0; i < arr.Length; i++) arr[i] = Int32.Parse(Console.ReadLine()); Console.WriteLine();
            //for (int i = 0; i < arr.Length; i++) Console.Write(arr[i] + " "); // Задание 1
            //for (int i = arr.Length-1;i>=0 ; i--) Console.Write(arr[i] + " "); // Задание 2
            //Array.Reverse(arr); foreach (int i in arr) Console.Write(i + " "); // Задание 2 вариант 2
            //int sum = 0; for (int i = 0; i < arr.Length; i++) if (arr[i] % 2 == 0) sum += arr[i]; Console.WriteLine(sum); // Задание 3
            //Console.WriteLine(arr.Where(i => i%2==0).Sum()); // Задание 3 вариант 2
            //Console.WriteLine(arr.Min()); // Задание 4
        }
    }
}
